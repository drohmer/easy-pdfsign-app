// Default styling
export const DEFAULT_TEXT_COLOR = '#1f2937'

// Default signature widths (fraction of page width)
export const DEFAULT_SIG_WIDTH_DROP = 0.18         // when image is dropped onto a page
export const DEFAULT_SIG_WIDTH_NO_DETECTION = 0.2  // when no auto-placement was detected

// Fallback signature position (fraction of page width)
export const FALLBACK_SIG_X = 0.6
export const FALLBACK_SIG_Y = 0.82

// Default x offset for added text fields (date, name, location, text, checkbox)
export const TEXT_FIELD_X = 0.1
